# Instrucciones para arrancar el cliente

#### Desde la terminal, acceder a la carpeta `client/` e instalar la dependencias

```ps
npm install
```

#### Arrancar servidor de desarrollo

```ps
npm run dev
```
